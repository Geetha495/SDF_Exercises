/*
 *  "."          Member access or dot operator
 *  "?:"        Ternary or conditional operator
 *  "::"        Scope resolution operator
 *  ".,*"       Pointer to member operator
 *  "sizeof"    The object size operator
 *  "typeid"    Object type operator
*/

/*
 *We can overload some operators, 
	because they are not evaluated by compiler at compile-time. Instead they are evaluated at run-time. 
 *Some operators cannot be overloaded 
	because they are evaluated at compile-time by the compiler and if they are overloaded, they cause serious problems in compiler and you do not get the desired output. In other words, they are not declared as abstract and polymorphism does not apply to those. 
*/


